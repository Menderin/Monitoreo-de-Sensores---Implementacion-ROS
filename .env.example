# ==============================================================================
# Monitor de Microalgas UCN — Variables de Entorno
# ==============================================================================
# Copiar este archivo como .env y completar los valores:
#   cp .env.example .env

# ─── MongoDB Atlas ─────────────────────────────────────────────────────────────
# Cadena de conexión completa (obtenida desde MongoDB Atlas → Connect → Drivers)
MONGO_URI=mongodb+srv://<usuario>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Nombre de la base de datos
MONGO_DB=Datos_ESP

# Colecciones
MONGO_COLLECTION=sensors_data
MONGO_COLLECTION_DISPOSITIVOS=devices_data

# ─── ROS 2 (opcional — solo si se requiere ajustar dominio) ────────────────────
# ROS_DOMAIN_ID=0
